Stratoshell:
    Task:
        Image: alpine-nodejs
        Cpu: 256
        Ram: 0.5
    Connection:
        Ports:
            - Port: 3000
              TargetPort: 3001
    PreSyncCommands:
        - node --version
    PostSyncCommands:
        - npm install
